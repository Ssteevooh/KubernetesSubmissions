apiVersion: apps/v1
kind: Deployment
metadata:
  name: the-project-dep
  namespace: project
spec:
  replicas: 1
  selector:
    matchLabels:
      app: theproject
  template:
    metadata:
      labels:
        app: theproject
    spec:
      volumes:
        - name: shared-volume
          persistentVolumeClaim:
            claimName: shared-claim
      containers:
        - name: theproject
          image: the_project
          envFrom:
            - configMapRef:
                name: project-config
          volumeMounts:
            - name: shared-volume
              mountPath: /usr/src/app/files
