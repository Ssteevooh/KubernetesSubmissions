apiVersion: v1
kind: Service
metadata:
  name: the-project-svc
  namespace: project
spec:
  type: ClusterIP
  selector:
    app: theproject
  ports:
    - port: 2345
      protocol: TCP
      targetPort: 3000
